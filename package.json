{
  "name": "mdpdf-desktop-assets",
  "private": true,
  "type": "module",
  "version": "0.1.3",
  "scripts": {
    "vendor": "node scripts/vendor_mermaid.cjs",
    "build:wasm": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build_wasm.ps1",
    "build": "npm run vendor && npm run build:wasm",
    "test:wasm": "node scripts/wasm_smoke.mjs",
    "bench:wasm": "node scripts/wasm_benchmark.mjs"
  },
  "devDependencies": {
    "mermaid": "11.16.0"
  }
}
